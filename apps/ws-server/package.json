{
  "name": "ws-server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot index.ts",
    "start": "bun run index.ts",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/redis": "workspace:*",
    "@perps-turbo-repo/env": "workspace:*",
    "redis": "^6.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.4",
    "typescript": "^6"
  }
}
