{
  "name": "db-puller",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "dev": "bun run --hot index.ts",
    "start": "bun run index.ts"
  },
  "dependencies": {
    "@perps-turbo-repo/db": "workspace:*",
    "@perps-turbo-repo/env": "workspace:*",
    "@repo/redis": "workspace:*",
    "dotenv": "catalog:"
  },
  "devDependencies": {
    "@perps-turbo-repo/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.21.9",
    "typescript": "catalog:"
  }
}
